# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- capball ---

# Generated demo media and scratch output — the repo ships no footage
tmp/
*.mp4
*.mkv
*.mov
*.m4v
*.webm

# Local database
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# FFmpeg sidecar binaries (detected or fetched, never committed)
src-tauri/binaries/

# Rust build output
src-tauri/target/

# Environment
.env
.env.*

# Local-only exclusions for plans/ and .zcode/ live in .git/info/exclude rather
# than here, so tools that hide gitignored files can still read the planning docs.
